{
  "name": "extpromise",
  "version": "0.0.0",
  "description": "ES6 promise extension",
  "main": "lib/index.js",
  "author": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "coffee --print src/*.coffee | babel --presets env > lib/index.js",
    "pub": "npm run build && npm publish"
  },
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "coffeescript": "^2.0.0-beta5"
  },
  "dependencies": {
    "es6-promise": "^3.2.1"
  }
}
